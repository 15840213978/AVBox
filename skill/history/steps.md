# AVBox 改造期实施记录（历史归档）

> 本文件是 `skill/history/` 归档的一部分：Step 0–7 渐进式改造的**实施记录与删除对账**（全部已完成）。
> 活规范见 `../avbox-mobile-ui-spec.md`，通用规则见 `../SKILL.md`。
> 用途：**仅在需要追溯某步改造的具体决策、删除清单、验证记录时按需检索**，不要通读。
> 说明：文内 `§x` 引用沿用归档前的旧编号（§4.x 未变；旧 §5/§6/§7/§8 已重组，见 SKILL.md 文档地图）。

---

## 原 §5 删除清单（Step 1 实际对账结果）
- **Manifest**:leanback feature、`app_banner` 引用(drawable 已删,直播页 LOGO 改用 app_icon)、LEANBACK_LAUNCHER、10 个 Activity 的 sensorLandscape → 全部改竖屏(Detail/LivePlay 保留 configChanges 防播放器重建)、PushActivity/PushReceiver 声明、xwalk meta-data ×2。
- **依赖**:xwalk(app/libs aar + proguard keep 规则)已删;`tv.recyclerview` **推迟删除**(用户确认:48 个存活文件仍用,随 Step 2~5 重写逐步清引用,Step 6 收尾卸依赖)。
- **整文件删除**:`SearchKeyboard`(连带 layout_keyborad/item_keyboard 布局)、`PushActivity`、`PushReceiver`、`QRCodeGen`、`RemoteDialog`(推送二维码,连带 dialog_remote.xml)、`XWalkUtils`+`XWalkInitDialog`(连带 dialog_xwalk.xml)、`DataReceiver`/`RawRequestProcess`/`InputRequestProcess`、raw 控制台页面(index.html/style.css/ui.css/jquery.js/script.js);**后续追加(2026-09-11)**:`LocalFileActivity`(自绘文件浏览器,改系统 SAF 后删除,连带 Manifest 声明与 `EXTRA_LIVE`)。
- **ControlManager/RemoteServer(用户确认改判)**:二者实为本地服务底座(爬虫/弹幕代理、M3U8 净化输出 /proxyM3u8、DOH /dns-query、/dash/、clan:// file 解析、ITV 代理),不能整删;只拆 web 控制台与推送链(静态页路由、/push、/action、/media、/upload、/newFolder、/delFolder、/delFile、InputRequestProcess)。`PlayFragment.getMediaInfo`(仅 /media 用)一并删除。
- **推送入口/二维码**:UserFragment tvPush(Java+布局)、SearchActivity 的 SearchKeyboard 触发与 remoteRoot 二维码块、ApiDialog 二维码/地址显示(api.html 页面本已不存在)。SearchActivity 的远程搜索提交已有 OnEditorActionListener,不受影响。
- **壁纸链**:ApiConfig.wallpaper 字段+解析、BaseActivity changeWallpaper/globalWp(改为固定 app_bg 窗口背景)、ModelSettingFragment llWp/llWpRecovery(Java+布局)。
- **按键分支(实际对账)**:
  - VodController:onKeyEvent 覆写、onKeyDown/onKeyUp 覆写、进度条 setOnKeyListener(DPAD 左右);孤儿代码 mLongPressRunnable/isLongPressTriggered/mmHandler/LONG_PRESS_DELAY/setMinPlayTimeChange/showUpBottom 同步删除。**触屏长按倍速 onLongPress/onTouchEvent 与 speedPlayStart/End 保留**(§4.4 手势);setOnTvLongClickListener 保留(10 处按钮使用,纯触屏行为不受影响)。
  - LivePlayActivity:dispatchKeyEvent(数字选台/频道切换/MENU)、onKeyDown/onKeyUp(确认键长按)、sBar 的 onKey **改写为普通 OnClickListener**(§5 的"DPAD_CENTER→普通 click");孤儿 divLoadEpg 左右/numericKeyDown/isFocusInView/focusCurrentGroupInMenu/focusChannelFromSelectedGroup 删除。
  - HomeActivity:dispatchKeyEvent(MENU 长按)删除,设置入口齿轮按钮保留。
  - DetailActivity:dispatchKeyEvent/onKeyDown/onKeyUp(转发 playFragment)、tvSeriesSort 焦点监听;PlayFragment 对应三个按键转发方法(孤儿)删除;MyWebView.dispatchKeyEvent(阻止聚焦)保留。
  - SearchActivity:etSearch.setOnKeyListener("监听遥控器",IME 提交已有 OnEditorActionListener)。
  - SettingActivity:dispatchKeyEvent(0000 dev-mode 触发)+ mDataRunnable + DevModeCallback/callback 链(连带 ModelSettingFragment 注册块);llDebug 隐藏至 Step 2 Compose 设置页直接呈现。FastSearch/Collect/History 无 dispatchKeyEvent/焦点记忆代码,仅功能性 requestFocus 保留。
  - LocalFileActivity 的 dispatchKeyEvent 未动(返回上级目录逻辑,§3:重写时改 OnBackPressedDispatcher)。
- **布局 XML**:62 个文件 nextFocus*/focusable/focusableInTouchMode 清理(284 处);6 个含 EditText 的文件保留防自动聚焦的 focusable 模式(activity_search、dialog_api、dialog_live_password、dialog_danmu_api、dialog_search_danmu、dialog_search_subtitle)。
- **Step 5 追加对账(2026-09-08)**:`LiveController`+`BaseController`+`player_live_control_view.xml` 退役(手势移植 ComposeLiveController,见 §4.5);7 个直播 adapter(LiveChannelGroup/LiveChannelItem/LiveEpg/LiveEpgDate/LiveSettingGroup/LiveSettingItem/MyEpg)+ `ViewObj`(面板滑入动画);8 个布局(activity_live_play、player_live_control_view、item_live_channel(_group)、item_live_setting(_group)、epglist_item、item_view_load_more);12 个 drawable(shape_live_*×4、bg_channel_list_*×2、item_bg_selector_left、icon_live、icon_play.png、vod_pause.xml+png、vod_play.png)。**保留**:`item_bg_selector_right`(styles.xml 仍引)、`anim_loading`(保守保留)、`LivePasswordDialog`+dialog_live_password(密码组继续用)、`EpgUtil`(EPG tag 名计算)。
- **Step 7 追加对账(2026-09-08)**:ApiDialog.java/ApiHistoryDialog.java/ApiHistoryDialogAdapter.java + dialog_api.xml/dialog_api_history.xml/item_dialog_api_history.xml 全部删除(替代实现 ApiSheets.kt,见 §8 Step 7)。**保留**:icon_delete(item_grid.xml 仍引)、input_dialog_api_input(dialog_live_password.xml 仍引)、CustomDialogStyleDim(孤儿 style,资源保留)。
- **验证点**:BaseController 手势(滑动快进/亮度/音量/双击暂停)代码未触碰;assembleDebug 通过(真机回归待装包验证)。

## 原 §7 实施路线（Step 0–7 已全部完成）
- [x] **Step 0 基建**:Kotlin+Compose 接入(验证 AGP9 内置 Kotlin 接法)、版本目录写入 §2 版本、M3 主题封装(浅/深/动态三态)、通用组件(SettingsGroup、骨架屏、三态、BottomSheet 封装、按压缩放卡片)
- [x] **Step 1 删 TV 代码**:按 §5 清单执行 + Manifest 竖屏化(播放页横屏策略见 §3)
- [x] **Step 2 MainActivity + 4 tab**:导航骨架;先迁移历史/收藏/设置(逻辑简单),首页最后(含 2026-09-07 上机联调补丁,见 §8 Step 2 实施记录)
- [x] **Step 3 搜索页**(系统输入法替换自绘键盘)
- [x] **Step 4 详情 + 播放页**(AndroidView 包 dkplayer 控制器;实施决策与记录见 §8 Step 4)
- [x] **Step 5 直播页**(ComposeLiveController + Compose 重写;实施决策与记录见 §8 Step 5)
- [x] **Step 6 收尾**:View 对话框 Compose sheet 化(2026-09-08 用户选方案 A:全部 sheet 化后卸 tv.recyclerview 依赖,一步到位)→ 旧 XML/Activity/依赖清理对账(§5 逐项核销)→ 全量回归(assembleDebug 通过;实施决策与记录见 §8 Step 6;真机回归待装包验证)
- [x] **Step 7(可选)**:ApiDialog/ApiHistoryDialog Compose 化(2026-09-08 实施,壳=AVBoxBottomSheet;实施决策与记录见 §8 Step 7 实施记录)

## Step 2–7 实施记录
### Step 7 实施记录(2026-09-08,两项决策已经用户确认)

- **决策 1|壳形态 = AVBoxBottomSheet**(评估记录两方案并列,用户选定):复用 BackupSheet/TextEditSheet 体系,输入框在 sheet 内有弹幕 API 先例;决策 2|「接口历史」Step 2 遗留 bug 顺手修复(git 考古确认旧 ModelSettingFragment 有完整数据逻辑,Step 2 被机械搬运成裸 `ApiHistoryDialog(it).show()` 弹空对话框,本次恢复)。
- **新文件 `ui/dialog/ApiSheets.kt`**:① `ApiSheetHost` 全局单例状态(visible 私有 setter,show()/dismiss() 公开;⚠️ 编译修复 1 处:私有 setter 外部不可赋值,勿在 onDismiss 里直接写 visible);② `ApiConfigSheetHost` 挂载在 `MainScreen.MainContent` 末尾;③ `ApiConfigSheet`(标题「接口配置」,历史直播/存储权限双按钮 + 点播/直播双输入行 + 选择×2 + 确定);④ `ApiHistorySheet`(LazyColumn,仓源「[仓]」前缀、当前项「√」前缀、当前项点击幂等不可删,列表上限 420dp 等价旧 vs_410)。
- **触发契约**:`Activity.showApiDialog()` 签名不变,仅置 `ApiSheetHost.show()`;确认/onchange 契约 1:1 内置 ApiConfigSheet.confirm():写接口历史 → 点播变更联动直播(等价旧 setText)→ 写 LIVE_API_URL → onchange(写 API_URL → 非线路历史清线路列表 → 变更时 AppBootstrap.retry)→ dismiss。设置页/首页/引导态三个调用方零改动。
- **ApiHistorySheet 双入口**:ApiConfigSheet 内「历史直播」= LIVE_API_HISTORY(空 toast「直播历史为空」,点击回填直播输入框+写 Hawk);SettingsPage「接口历史」= API_HISTORY(恢复旧逻辑:点击应用写 API_URL+LIVE_API_URL+setLiveApiHistory+清线路+vm.refresh+变更时 retry;删除写回;空历史不弹)。
- **IME 链**(评估方案③落地):点播框 Next → FocusRequester 聚焦直播框;直播框 Done → clearFocus 收键盘;旧 focusConfirm 的「焦点移到确定按钮」属 TV 焦点链,不迁移。
- **死代码发现与等价删除(记录在案)**:TYPE_API_URL_CHANGE/TYPE_LIVE_API_URL_CHANGE 两个 EventBus 事件全库无发送方(仅 ApiDialog 与 HomeViewModel 订阅),ApiDialog 侧 EventBus 订阅等价删除;HomeViewModel 同名订阅保留(不在本步范围);`setLocalApi()` 孤儿方法随删。
- **行为差异**:setCanceledOnTouchOutside(false) → sheet 点外部关闭(与 Step 6 各 sheet 一致)。
- **删除对账**:ApiDialog.java(204 行)/ApiHistoryDialog.java/ApiHistoryDialogAdapter.java + dialog_api.xml/dialog_api_history.xml/item_dialog_api_history.xml,净 -670 行。**保留**:icon_delete(item_grid.xml 仍引)、input_dialog_api_input(dialog_live_password.xml 仍引)、CustomDialogStyleDim(成孤儿 style,资源保留不动)。
- **验证**:assembleDebug 通过(134 tasks,2 轮;第 1 轮 1 处编译错见上);真机回归点:设置页接口配置输入/确定后首页重载、历史直播选择回填直播输入框、存储权限申请、设置页接口历史列表应用与删除(新恢复功能)、首页引导态「配置接口」与 Tune 入口、本地选择回跳。

### Step 7 评估记录(2026-09-08 评估,同日已实施,见上实施记录)

- **现状盘点**:ApiDialog.java 204 行 + dialog_api.xml 单布局(居中面板,无 WebView/无 TvRecyclerView),子对话框 ApiHistoryDialog 的布局在 Step 6 已换 androidx RecyclerView。入口统一为 `Activity.showApiDialog()`(ui/page/Jump.kt),调用方:SettingsPage「接口配置」行 + HomePage 两处。业务:点播/直播两个接口输入框 + Hawk 读写(API_URL/LIVE_API_URL/历史)+ EventBus 订阅 RefreshEvent(TYPE_API_URL_CHANGE/LIVE_API_URL_CHANGE 回填输入框)+ 存储权限申请 + 直播历史选择(ApiHistoryDialog)。
- **风险结论**:**低~中,工作量约半天**。比 Step 6 预估的风险面小:无 Web 粘贴面板、无复杂焦点动画;唯一需谨慎的是 `onchange` 回调触发 Home 侧整条配置加载链,迁移后必须保持「确认 → 写 Hawk → 回调 → dismiss」时序不变。
- **实施方案(建议)**:①新建 Compose Dialog(同 PlayerSheets 的独立 Dialog 窗口模式)或直接挂在 SettingsPage 的 AVBoxBottomSheet,双 OutlinedTextField + 四按钮行,视觉对齐 dialog_panel 体系;②EventBus 订阅改 DisposableEffect 内注册,或直接用单例状态流替代(仅两处回填);③IME 的 NEXT/DONE 焦点链用 KeyboardActions + FocusRequester 实现;④`showApiDialog()` 扩展函数签名不变,内部替换实现,调用方零改动;⑤ApiHistoryDialog 同步 sheet 化(数据逻辑不变,仅壳替换)。

### Step 6 实施记录(2026-09-08,方案 A 已经用户确认)

- **播放器对话框**:6 个 View 对话框(弹幕设置/弹幕搜索/字幕/字幕搜索/投屏/选集)+ 3 个 SelectDialog 轨道选择器全部改走 `PlayerUiState` 新增的 6 个 sheet 状态类 + `PlayerControlApi.getUiState()` 桥接,UI 统一在新建 `player/ui/PlayerSheets.kt`(~1000 行:SheetPanel/ChipRow/Stepper/Input 等组件 + 6 个 sheet)。选集 sheet 复刻旧 getSpanCount 的 Paint 测宽逻辑(1~4 列自适应)+ 右半屏面板/左半屏点按关闭;投屏 sheet 用 LinkedHashMap 去重 + DisposableEffect(scanToken) 自动扫描,失败仅 toast 不关闭(照旧行为);字幕搜索 sheet 用 `androidx.lifecycle.viewmodel.compose.viewModel()` + `LocalLifecycleOwner.current` 观察 SubtitleViewModel(lifecycle 2.11 KMP 已废 ViewModelProvider(activity) 构造)。Java 侧 lambda 返回 `kotlin.Unit.INSTANCE`,且不能与裸 `return;` 混用。
- **设置/搜索类**:BackupDialog → SettingsPage 内 BackupSheet(列表+恢复+删除);BackupManager 新建(备份 Hawk2+crypto JSON,保留 10 份,恢复后重启)。零引用孤儿直接删:DanmuApiDialog/TipDialog/GridFilterDialog/SearchCheckboxDialog/SelectDialog 及连带 adapter/布局。
- **页面重写**:`FastSearchActivity.kt`(同片名聚合快搜,调度引擎 1:1 迁移至 `FastSearchEngine.kt`:6 线程泵/批量推进/10s 超时/暂停恢复全保留)+ `LocalFileActivity.kt`(本地文件浏览);旧 Java/布局/adapter 删除。
- **保留 View 的偏差**:ApiDialog(接口配置,核心流程高风险)+ 其子 ApiHistoryDialog 不迁移;ApiHistoryDialog 布局的 TvRecyclerView 换 androidx RecyclerView 以为卸依赖铺路。可作可选 Step 7。
- **依赖卸载**:tv-recyclerview、base-RecyclerViewAdapterHelper 迁移后零引用,从 build.gradle.kts + version catalog 全部移除;Jetifier 暂留(gradle.properties 已备注可关条件)。
- **⚠️ zxing-core 误删已回滚(2026-09-12)**:当初 zxing-core 按"宿主源码零引用"一并移除是**误判**。它是**运行期动态依赖**:爬虫 jar 由 `JarLoader`(`app/src/main/java/com/github/catvod/crawler/JarLoader.java:89`)以 `DexClassLoader(jar, cachePath, cachePath, App.getInstance().getClassLoader())` 加载,父加载器即宿主,jar 生成二维码时引用 `com.google.zxing.EncodeHintType` 必须由宿主提供。缺失后 2026-09-12 19:52 设备更新爬虫 jar,应用启动约 10s 主线程必崩(`NoClassDefFoundError: com.google.zxing.EncodeHintType`,栈全在 `com.github.catvod.spider.merge.*`)。已在 version catalog + `app/build.gradle.kts` 恢复 `com.google.zxing:core:3.5.4`(与 `示例文件/TV-fongmi` 的 `catvod` 模块 `api libs.zxing.core` 设计一致);`app/proguard-rules.pro:178` 的 `-keep class com.google.zxing.**{*;}` 保留生效。**约束:依赖裁剪不得只看宿主源码静态引用——凡动态加载 jar 可能用到的宿主 API(gson/okhttp/zxing 等)一律保留。** 完整诊断见 `logs/crash_diagnosis_20260912.md`。
- **删除对账**:12 个布局全部去 TvRecyclerView 化;删 dialog/adapter Java 12+、布局 XML ~23 个、ShellUtils 孤儿、proguard org.chromium 死规则、dialog_select.xml(含死 TvRecyclerView tag,不删会在资源合并报错)。
- **运维|Chaquopy 断网方案**:系统代理连 pypi.org/chaquo.com 全超时导致 installDebugPythonRequirements 连续 5 轮失败。解法:①`pyramid/build.gradle.kts` pip options 加 `-i https://mirrors.aliyun.com/pypi/simple/`(纯 Python 包直连)+ `--find-links` 本地 wheelhouse;②wheelhouse 在 `pyramid/wheels/chaquopy-prebuilt/`,由 `extract_wheels.py` 从 pip http-v2 缓存提取 79 个 wheel(含 lxml/ujson/pycryptodome 的 Android 平台 wheel 及 libxml2/libxslt/libcxx 原生依赖,双架构);③坑:pip 25.3 对 find-links 本地目录用严格文件名解析器,Chaquopy 原生库包名带连字符(chaquopy-libxml2)被判 invalid version,需改名为下划线形式(pip 的 canonicalize 视两者等价,安装校验可过),并生成 index.html 兜底(gen_index.py)。注意 build/ 被 clean 后 wheelhouse 不受影响(已放在 wheels/ 持久化目录)。
- **验证**:assembleDebug 通过(134 tasks);修复 4 处编译错:SheetChipRow 尾随 lambda 绑到 modifier(改具名参数)、ViewModelProvider(activity) 失效(改 compose viewModel())、Java lambda 裸 return 混用、Icons.Filled.Delete 扩展属性不能全限定访问(必须 import)。真机回归新增点:播放器 6 sheet + 3 轨道选择器、备份 sheet 恢复/删除、快搜聚合与源过滤、本地文件浏览。

### Step 5 实施记录(2026-09-08,四项决策已经用户确认)

- **决策 1|控制器**:LiveController 同法 Compose 化为 **`player/controller/ComposeLiveController.kt`**(C1 思路):`getLayoutId()=0` headless 无 UI,职责仅剩手势桥(单击/长按/左右快滑/亮度音量,灵敏度照抄旧 LiveController+BaseController)+ onPlayStateChanged 转发;loading/时移条/全屏浮层全部由 Activity 的 Compose 层渲染。**注意**:initView 由父类构造函数虚调用,控制器内经 initView 赋值的字段必须 `lateinit`(Kotlin 属性初始化器在 super 构造后才执行,带 `= null` 初始化器会覆写);已 javap 验证 ComposeVideoController 因 Kotlin 对 final 类默认值初始化器的省略优化而同样安全,但该风险在未来重构(给相关字段加非默认初始化器)时会复现。
- **决策 2|设置入口**:播放器角标 → 直播设置 bottom sheet(右滑入面板废弃);决策 3|时移 UI = 播放器底部简化时移条(点画面显隐);决策 4|全屏同详情页。
- **重写 `LivePlayActivity`(同名同包 Kotlin+Compose,继承 BaseActivity,getLayoutResID 复用 activity_main)**:EPG 加载/解析(JSON+XML+缓存 hsEpg+多查询名回退+默认地址兜底)、时移回看 URL 构建(catchup source/regex/token 全套)、直播配置加载链(shouldReloadLiveConfig/loadLiveConfigOnEnter/loadProxyLives py+js 爬虫分支/TxtSubscribe)、频道播放链(playChannel 复用播放器判断/自动换源状态机 3.5s+连接超时)、密码锁分组、7 组设置数据等业务逻辑 **1:1 移植**;页面三态 LOADING/EMPTY(带重试)/READY。
- **行为变化(记录在案)**:① 左右快滑由旧「换源」改为 §4.5 定稿的「切上一/下一频道」(换源保留在设置 sheet 线路选择+自动兜底);② 返回键退出回看不再顺带 playPreSource 换线,改为回直播当前源(旧行为对单源频道实际无效,判定为顺带 bug);③ 底部 EPG 信息条改常驻信息区(TV 浮层 6s 自动隐藏废弃);④ 清晰度角标补 3s 自动隐藏(旧常驻);⑤ TV 焦点机制(焦点记忆/ViewObj 动画/数字选台浮层)全部不迁移;⑥ EPG 空态页补重试按钮(旧空白)。
- **删除**:旧 LivePlayActivity.java(3120 行)、LiveController.java、BaseController.java、ViewObj、7 个 adapter、8 个布局、12 个 drawable(详见 §5 Step 5 对账);`LivePasswordDialog`/`EpgUtil`/`LivePlayerManager`/直播 bean 全部保留。
- **验证**:assembleDebug 通过;真机回归点:进入直播自动播上次频道/切台快照与清晰度角标/折叠列表换台与密码组/左右快滑切台/亮度音量手势与指示/EPG sheet 回看+回直播/时移条 seek 与自动隐藏/设置 sheet 七组功能/全屏沉浸与返回层级/多源与配置切换刷新/空态重试。
- **运维备注**:后台 gradle 卡住无输出时,先检查遗留 daemon 进程(`Get-Process java`)并 kill(本次两个昨日 daemon 占锁导致);`-q` 模式下编译成功不产生任何输出,不能以「无输出」判断卡死。

### Step 4 实施记录(2026-09-07,四项决策已经用户确认)

- **决策 1|播放链路迁移**:`PlayFragment` 改造为非 Fragment 的 View 容器 **`ui/player/PlayContainer.java`**(extends FrameLayout implements CustomAdapt),播放内核逻辑(解析/嗅探/自动换线/重试/字幕/弹幕/投屏/音乐会话)整体保留;`MusicPlaybackService` 的 `PlayFragment` 类型全部改签名 `PlayContainer`;布局 `activity_play.xml` 改名 `view_play_container.xml`。生命周期由宿主显式驱动:`hostResume()/hostPause()/hostDestroy()`(注意 `mActivity` 置空必须在 `stopLoadWebView` 之后,否则 WebView 泄漏)。`SourceViewModel` 由 ViewModelProvider 取改为直接持有实例(容器非 ViewModelStoreOwner)。
- **决策 2|菜单对话框**:本步保留 View 版(EpisodeDialog/SubtitleDialog/SearchSubtitleDialog/DanmuSettingDialog/SearchDanmuDialog/CastDeviceDialog/SelectDialog/解析行),挂在旧 BaseActivity 体系上正常弹出;**sheet 化留作后续独立小步**。
- **决策 3|换源交互**:详情页「换源」行 = 进入详情即后台聚合搜索(复用 getSearch + TYPE_SEARCH_RESULT,token= `detail_N` 防串,并发限流 6、单源 30s 超时),同名结果以 chips 展示(过滤 used/当前源),点击 `loadDetail` 切换;**旧自动兜底保留**(线路耗尽 → 容器回调 `startDetailFallbackAfterLinesExhausted` → 候选池顺序自动加载,单候选 6s 超时)。旧 detail fallback 的 20 线程分批搜索链路简化为统一聚合搜索,行为对齐。
- **决策 4|相关推荐** = 聚合搜索的**非同名**结果(与换源同一次搜索,数据互补),海报横排(VodCard 110dp),点击 `jumpToDetail` 开新详情页。
- **重写 `DetailActivity`(同名同包 Kotlin+Compose,继承 BaseActivity)**:顶 16:9 播放器(AndroidView 包 PlayContainer,竖屏=预览态、全屏=占满)→ 标题/元信息(来源·年份·地区·类型)/收藏心形 → 简介可展开(内联,DescDialog 删除)→ 清晰度 chips(仅多清晰度)→ 线路 chips(点击直接播匹配集,旧版仅切列表)→ 选集横向行(倒序/全部按钮;滚动定位当前集)→ 换源行 → 相关推荐。选集「全部」= bottom sheet(线路 chips + 分组 chips + LazyVerticalGrid 72dp 自适应,当前集高亮;分组规则 size>400→120、>100→60、否则 20,旧 span 自适应计算简化)。
- **全屏策略**:点播放器(竖屏透明点击层,预览内不响应控制器手势,同旧 block 设计)→ `SENSOR_LANDSCAPE` + 基类沉浸(`hideSysBar` 仅全屏时走 super);返回 → 控制器菜单可见先收菜单,再退回竖屏。configChanges 防重建,字幕预览态 0.6 倍(旧 toggleSubtitleTextSize)。
- **同步机制**:DetailViewModel 观察 `detailResult` LiveData(⚠️不可漏,否则页面死等)+ EventBus `TYPE_REFRESH`(VodInfo 同步回写/播放器配置)+ `TYPE_PLAY_QUALITY`(清晰度行);`preparePlayBundle` 复刻旧 jumpToPlay 的 previewVodInfo 镜像 + `App.setVodInfo` + 历史写入契约;播放经 `playSignal` + `LaunchedEffect(container, playSignal)` 在容器就绪后发起(Activity 重建自动补播)。
- **删除**:旧 DetailActivity.java(2056 行,含换源引擎/选集焦点逻辑)、PlayFragment.java、activity_detail.xml、DescDialog+dialog_desc、QuickSearchDialog+dialog_quick_search+item_quick_search_lite、SeriesAdapter、SeriesFlagAdapter、item_series_flag。播放地址行/复制功能随布局移除(§4.4 未列)。
- **连带简化(记录在案)**:SHOW_PREVIEW 恒为预览模式(新设置页已无此项);§4.4 的「评分」不显示(源数据无评分字段、豆瓣未接入);旧 tvSeriesSort 的 firstReverse/焦点链、mEmptyPlayList 由空态组件替代;快搜词 chips(SearchWordAdapter 分词)随 QuickSearchDialog 移除,FastSearchActivity 保留。
- **验证**:assembleDebug 通过;真机回归点:内嵌播放/全屏旋转沉浸/选集换集/换源 chips 与自动兜底/相关推荐跳转/收藏/历史进度恢复/字幕弹幕对话框/投屏。
- **补丁(2026-09-07,装机反馈)**:① 换源 chips label 由片名改为**源名称**——候选片名全部相同,展示片名导致整行重复「剑来」;② 选集 sheet 列数由 Adaptive(72dp) 改为**按最长集名估宽动态定列**(2~6 列,fontScale 感知,旧 refreshList 文本测宽思路)——部分源的集名带后缀,固定列宽截断成「第2…」;③ 换源后选源行滚回行首(LazyRow 滚动位置跨数据更新保留,当前源 chip 被带出视野);④ 竖屏播放器下移至状态栏下方(statusBarsPadding),视频不再顶到状态栏,全屏横屏沉浸不变(全屏时 inset 自动归零);⑤ 状态栏区域定稿**纯黑**(用户改选),播放器紧贴其下,状态栏图标强制白色(`isAppearanceLightStatusBars=false`,深浅色主题一致)。
- **补丁(2026-09-08,装机反馈)**:⑥ 选集 sheet 二次定案:**LazyVerticalGrid Adaptive(64dp) 自适应多列**(替代 9/8 早间的单列一行一集方案——该方案为防长集名截断而生,但纯数字集数一行一个严重浪费空间)。短集名一行 4~6 个、长集名在单元格内跑马灯不丢信息;保留 lazy 虚拟化与 scrollToItem 定位当前集(LazyGridState);芯片 40dp 高 + fillMaxWidth。至此选集 sheet 列策略三易其稿:72dp 网格 → 最长集名动态定列(2~6)→ 单列 → 自适应多列网格+跑马灯(定稿)。
- **补丁(2026-09-11,装机反馈)**:⑦ 详情页 chips 分区行(「清晰度」/「线路」)统一改为 **surfaceBright 圆角卡片容器**(圆角 16dp、距屏 6dp、卡内 vertical 12dp、标题 start 16/bottom 8、chips 行 contentPadding 16),规格与选集/换源卡完全一致;chips 颜色不变(默认描边透明底,选中态 M3 默认)。实现位置 = `DetailActivity.ChipRow`(清晰度与线路共用,故两行同时卡片化)。
- **补丁(2026-09-11,装机反馈)**:⑧ 详情页状态栏图标"消失"修复。现象:快速从横屏全屏退出回竖屏后,顶部状态栏只剩黑底(时钟/电量等系统图标不可见,黑底与播放器连成一片)。诊断:黑底本身是 ⑤ 的既定设计(非 bug);异常在于**图标外观被系统按主题重设**——系统 ROM(装机实测设备 vivo V2425A / OriginOS)在沉浸退出/横竖屏过渡与回前台时会重设状态栏图标外观(浅色主题 → 深色图标),深色图标画在纯黑底上等于消失(与 `MainActivity.onResume` 2026-09-11 修的"系统按主题重置外观"同类)。修复:`DetailActivity` 抽出 `applyStatusBarAppearance()`(恒 `isAppearanceLightStatusBars=false`),在 `init` / `applyFullscreen(false)`(同步断言 + `decorView.postDelayed` 400ms 过渡后再断言一次,常量 `SYSBAR_APPEARANCE_REASSERT_DELAY_MS`)/ `onResume` 三处反复断言。⚠️ **同类风险未处理**:`LivePlayActivity` 竖屏同为"纯黑状态栏 + 白色图标"结构(init/applyFullscreen(false) 各断言一次),存在同款问题,待用户确认后同样加兜底。
- **补丁(2026-09-11,装机反馈)**:⑨ 详情页「线路」卡(及共用的「清晰度」卡)宽度对齐「选集」卡。现象:只有两条线路时卡片明显比下方选集卡窄一截(装机截图)。根因:`ChipRow` 的卡片宽度由最宽子项决定——标题是 wrapContent 的 `Text`(「线路」两字),`LazyRow` 在 chips 放得下时也是 wrapContent,两者都不给宽度 → 卡片缩到标题/chips 宽度;而选集卡(`EpisodeRow`)与换源卡(`SourceSection`)是靠表头 `Row` 的 `fillMaxWidth()` 把 `Column` 撑满的(⑦ 说的"规格完全一致"漏了宽度这一项)。修复:`ChipRow` 显式给 `Column` 与 `LazyRow` 各加 `Modifier.fillMaxWidth()`——卡片与选集卡等宽(距屏 6dp 不变),且横向滑动的手势区覆盖整条卡片而非只有 chips 那几个字的范围;**线路多时行为不变**(`LazyRow` 按视口宽度滚动,超出部分横向滑动查看,`fillMaxWidth` 不影响该行为)。
- **诊断记录(2026-09-12,已定论+已修复):播放时滑动/开 sheet"卡顿掉帧"的真因 = Exo 把视频帧率写进播放 Surface,ROM 据此把整机刷新率降到 60Hz**。窗口级申请压不住(见下"已否决的方案"),最终从**源头**关闭该帧率提示(见下"最终修复")。现象(用户报"竖屏详情页播放时滑动下半屏/弹 bottom sheet 卡顿掉帧,暂停就正常,直播页同样")。**真机实测定位(adb + gfxinfo + dumpsys display/SurfaceFlinger,先后推翻三轮猜测:弹幕 / 4K 解码 / GPU 带宽)**:
  - **不是重组**:滚动 120 帧样本 UI 线程 p50 **0.81ms** / p90 1.01ms / max 1.43ms(真有重组风暴这里会是 8~20ms)。
  - **不是主线程阻塞**:同一样本 **0/120 帧**超 FrameDeadline;gfxinfo 汇总 299 帧现代 Janky **1 帧(0.33%)**、Missed Vsync 0;整帧 p50 7.7ms / p90 10.4ms(预算 16.67ms)。
  - **不是解码/GPU/带宽**:视频层实际分辨率仅 **864×362**(播放器角标同值),且走 **HWC 硬件叠加**(`dumpsys SurfaceFlinger` 合成列表 `DEVICE`、`hwc: composition=DEVICE`),App 侧 GPU 仅 2.4ms —— 这也解释了用户"换低分辨率源也一样"。
  - **真因①(主因):播放时系统"智能刷新率"把显示降到 60Hz**。`mActiveModeId=4 / mActiveRenderFrameRate=60.000004`(暂停后回到 `mode 1 / 120.00001`)。同段滑动实测渲染帧率 **播放 59fps ↔ 暂停 116fps** —— 主观"卡"来自刷新率腰斩而非掉帧。
  - **真因②(次要,仍待办):开 bottom sheet 有真实的 ~58ms 首帧停顿**(整帧 69ms,其中"遍历前"Compose 重组 37.6ms + 遍历 20.6ms),超 deadline 被记为 janky;sheet 内滚动正常(p99 13ms)。⚠️ 未区分"仅首次开(首次组合/类加载)"还是"每次都卡",需连开 5 次抓逐帧。
  - **已否决的方案(不要再走)**:窗口级高刷申请(`preferredDisplayModeId` / `preferredRefreshRate` / API 34+ `View.setRequestedFrameRate`)。装机实测无效:申请确已发出、开关确已打开,播放时显示仍被降到 60Hz —— **ROM 按视频内容投票的降频优先级高于 App 窗口请求**。功能已完整删除。
  - **最终修复(2026-09-12,用户定稿:底层默认开启,不设开关)**:关键线索 = **IJK 内核播放时刷新率不降,只有 Exo 降**。根因定位到 media3:`VideoFrameReleaseHelper` 默认把视频帧率写进播放 Surface(`Surface.setFrameRate`,策略默认 `C.VIDEO_CHANGE_FRAME_RATE_STRATEGY_ONLY_IF_SEAMLESS`,源码 1.9.0 `:140`/`:460`),系统据此做显示模式匹配,ROM 进一步把整机降到 60Hz;IJK 不调用该 API 故不触发。修法 = 官方出口 `Renderer.MSG_SET_CHANGE_FRAME_RATE_STRATEGY`(=5,public)+ `C.VIDEO_CHANGE_FRAME_RATE_STRATEGY_OFF`,下发后 media3 内部 `updateSurfaceMediaFrameRate/clearSurfaceFrameRate` 直接 return(源码 `:364-400`),不再有任何 `Surface.setFrameRate` 调用。实现:`com.github.tvbox.osc.player.ExoPlayer.disableFrameRateMatching()` —— 其 `SubtitleOffsetRenderersFactory` 在 `buildVideoRenderers()` 捕获视频渲染器(工厂构造多一个 `List<Renderer>` 参数收集),`initPlayer()` 里逐个下发 OFF;无设置项、无 Hawk 键。代价:放弃"显示刷新率随视频帧率匹配"(24p→24Hz 这类);手机高刷屏上 24/30p 在 120Hz 下本就整除,影响很小。若需恢复 media3 默认行为,删掉 `initPlayer()` 里的 `disableFrameRateMatching()` 调用即可。
  - ⚠️ **若该机型仍降频**(说明 ROM 看的是另一个 Exo 独有信号 `MediaFormat.KEY_OPERATING_RATE`,media3 按视频帧率设置、IJK 不设,源码 `:1480-1488`):备选 = 在自定义 codec 适配层屏蔽该 hint,或将该 ROM 的默认内核设为 IJK。
  - 可选的系统侧绕过(未采纳,仅记录):系统设置里的刷新率档位/开发者选项强制高刷;或 `settings put system min_refresh_rate 120`(需 shell 权限,非普通 App 可写,且属全局副作用)。另外窗口级申请已证明无效,不要再试。
- **补丁(2026-09-11,用户定稿方案 C):换源点击即停 + 失败回滚原源**。现象(用户提问确认):竖屏详情页点「换源」后,旧源播放**不会被暂停、也不会被结束**——`switchSource()` 只改 ViewModel 状态并发起新源详情请求,播放容器毫不知情;旧源一直播到新源详情返回后 `setData → play(false) → mVideoView.release()` 才停。而竖屏 `pageState=Loading` 会在播放器上盖一层**不透明纯黑 Box + spinner**(`DetailScreen`,overlay 条件 `!full`),于是表现成"画面黑了/转圈了却还有旧源声音",窗口长度 = 新源详情耗时(HTTP 源 OkGo 超时 10s;JS/spider 源 `getDetail` 手动路径 `future.get(30s)`;新源详情为空转自动兜底则每个候选 6s,期间持续黑罩+旧源出声;兜底全败进空态时旧源**仍在播**,且空态不再盖罩→画面重新露出、下面却是"暂无片源")。整条链路此前没有任何一处 pause/release(全文只有 `onPause → hostPause`)。
  - 实现(方案 C:反馈最干脆,失败靠回滚兜住):①`PlayContainer.stopForSourceSwitch(tip)` —— 立即停播:先 `getCurrentPosition()` 刷新 `mCurrentPosition`(⚠️`VideoView.release()` 的 `saveProgress` 读的是该字段,不刷新会把上次查询的旧值/0 落盘),再 `pause + release + stopParse + cancelPlayTimeout + stopMusicSessionForFailedPlayback + mController.stopOther + resetDanmuState`,并置 `switchStopPending` 抑制在途取流结果(`playResultObserver`)与已排队的 `goPlayUrl` 回调把旧源重新拉起;提示走 `PlayerTipBridge`("正在切换片源",盖在 Loading 黑罩之上)。②进度继承:`pendingInheritKey/pendingInheritProgress` 记下停播键与位置,新源 `play()` 时喂给既有 `inheritProgressKey/inheritProgress` → `inheritProgressIfNeeded` 写进**新源 progressKey**(新键已有历史记录则不覆盖,与自动换线同规则);回滚原源时键相同,release 已落盘的位置直接复用。③回滚(`DetailViewModel.SwitchSnapshot` + `rollbackManualSwitch`):点换源时快照(vodInfo/vodId/sourceKey/firstsourceKey/片名/海报),新源**有可播列表**才作废;目标源报错(`onDetailResult` 非空 msg / `handleEmptyDetail` 应关页分支)、源不可用(`onDetailUnavailable`)、自动兜底全败(`finishFallbackWithoutResult`)四处改为**回退原源 + 从停播处续播**并 toast「换源失败:原因,继续原片源」,不再直接进空态/关页;无快照(非手动换源)时保持原有空态/关页行为。④快照不覆盖已存在的快照:换源窗口内连点、或"能解析但无可播集"的中间态下,回滚目标始终是**最后一次可播状态**。⑤`enterEmpty()` 统一进空态并顺带 `clearSourceSwitchTip()`(容器已随停播置起提示,否则空态页会永远挂着"正在切换片源"spinner);`playCurrent()` 组不出 bundle 时同样清提示。
  - **行为变化(有意)**:换源窗口内**静音**(旧行为是旧源继续出声);换源失败不再停在空态/关页,而是回原源续播;手动换源现在**延续播放进度**(此前 `progressKey` 换源即变、`inheritProgressKey` 只由自动换线 `tryNextLine` 填充,手动换源从 0 开始——与自动换线不一致,本次一并接上)。
  - 验证:`assembleDebug` 通过。真机回归点:换源成功(进度续播/无旧源残响)/目标源报错回滚/兜底全败回滚/连点换源/换源后返回退出。
- **补丁(2026-09-11,用户定稿方案 B):sheet 由 dialog window 改为应用窗口内覆盖层**。现象:详情页点「选集→全部」等任何 sheet 弹出时,顶部状态栏图标闪烁两次(稳定复现);`AVBoxBottomSheet` 原用 M3 `ModalBottomSheet`(独立 dialog window),弹出/关闭时状态栏"外观归属窗口"切换两次,浅色容器使图标先变深色再被 `SideEffect` 改回白色 → 闪两次;且该 `SideEffect` 强制白色会让**主页等浅色页面**开 sheet 时图标被写成白色(隐患)。
  - 实现(`ui/components/BottomSheet.kt` 重写):`AVBoxBottomSheet` = 就地渲染的 `SheetOverlay`(`Box(fillMaxSize)` + scrim + 底部面板,28dp 顶圆角、`SheetMaxWidth=640dp`、拖拽把手、`navigationBarsPadding`),动画与手势全在应用窗口内完成,不再触碰状态栏外观;支持 scrim 点击/返回键/下滑拖拽关闭(用户主动关闭先滑出再回调,程序化关闭仍是瞬时移除,与旧行为一致);新增 `SheetHostState`/`LocalSheetHost`/`SheetHost` 槽位——页面位于被裁剪容器(MainScreen 的 pager tab)时,`MainScreen.MainContent` 在窗口根部提供槽位,页面内 sheet 自动改在该处渲染(可覆盖底栏与系统栏),调用方零改动。M3 `ModalBottomSheet`/`SheetState`/`rememberModalBottomSheetState` 及原 dialog 窗口外观 hack 全部移除。
  - **坑(记录在案)**:① 入场动画**不要**依赖 `onGloballyPositioned` 的测量回调(先量后滑),一旦回调缺失/被打断,面板永远停在屏外,表现为"点 sheet 不弹";改为比例式(0~1)收起量,面板高度由 `graphicsLayer` 的 `size` 在绘制期取,`translationY = collapse * size.height`。② 遮罩在入场完成前必须 `clickable(enabled = entered)`,否则 `alpha=0` 的隐形遮罩会吃掉整页触摸(sheet 看似没弹+页面点不动)。③ **LivePlayActivity 的播放器 sheet(PlayerSheets.kt)仍是 `Dialog`**,视觉是居中面板、处于全屏无系统栏环境,本次不动。
  - 验证:`gradlew installDebug` 通过;设备 dex 校验新代码已进包;落盘日志确认覆盖层组合 + 入场动画完成(`panelHeightPx=2369/1823`)。
  - **补丁(2026-09-11,装机反馈)**:拖拽把手横向居中 —— `BottomSheetDefaults.DragHandle()` 自身仅 32dp 宽(内部用 `align(Center)` 定位),直接作为 Column 子项会贴到面板左边;需外层套 `Box(Modifier.fillMaxWidth(), contentAlignment = Center)`。
  - **补丁(2026-09-11,装机反馈)**:底部沉浸 —— sheet 内容区**不加 `navigationBarsPadding()`**(用户定稿):列表可滑到手势条下面、手势条浮在内容上;面板底色本已铺到屏幕最底,各 sheet 内容自带 16dp 底部 padding 保证收尾间距。若某 sheet 的内容需要避开手势条,由该 sheet 自己在内容里加底部 padding,不在共享壳层统一加。
  - **补丁(2026-09-11,装机反馈):筛选 sheet「确定」够不到 —— 手写覆盖层丢掉 M3 的面板限高与内容滚动**。现象:电影/电视筛选点开后,筛选组很长(类型 21 项 + 地区 31 项 + 年份…)时,内容被裁在屏幕外且**无法下滑**,排在内容最末端的操作行「清除/确定」永远不可见。根因(即上述 2026-09-11 覆盖层重写的回归):①`SheetOverlay` 的面板无 `heightIn`,仅受父级 `fillMaxSize` 约束,内容超出即被裁;②`content` 槽位无 `verticalScroll`/`LazyColumn`,没有任何滚动容器承接;③`draggable` 挂在**整个面板**上,即使内层可滚也会被"下滑关闭"抢走触摸。M3 `ModalBottomSheet` 原版自带"限高 + 内容滚动",这次改写把这两个能力一并丢了,而 `LivePlayActivity` EPG/直播设置、`HomePage` 订阅源、`DetailActivity` 选集各自用 `LazyColumn(...heightIn)` 手工兜底,唯独 `FilterSheet` 漏了,故只有筛选暴露。修复(共享壳层统一兜底,`ui/components/BottomSheet.kt`):①面板加 `heightIn(max = 屏幕高 × 0.9)`(对齐 M3 默认上限,常量 `SheetMaxHeightFraction`);②把手/标题固定,内容区改为 `weight(1f, fill = false) + verticalScroll`——短内容面板自适应,超高把面板顶到限高并在区内滚动;③下滑关闭手势从整个面板**收窄到把手/标题区**,不再与内容区纵向滚动抢触摸;④新增 `isScrollable: Boolean = true` 参数,内容自带滚动容器的 4 处 sheet(`HomePage` 订阅源 420dp、`LivePlayActivity` EPG 520dp / 直播设置 560dp、`DetailActivity` 选集 grid)传 `false`,由内层自己滚。**行为变化(有意)**:内容区不再能拖动关闭 sheet,关闭方式 = 拖把手/标题、点遮罩、返回键三种;点击内容不误触关闭。
  - **教训(写进 §4.3 组件约定)**:手写替代 M3 组件时,必须逐项核对被替代者**隐式**提供的能力(限高、滚动、手势仲裁),否则回归只在"内容足够长"时才暴露。共享壳层应把这些能力做成默认值,而不是依赖每个调用方自觉兜底。

### Step 3 实施记录(2026-09-07)

- **重写 `SearchActivity`(同名同包,Compose,继承 BaseActivity 适配旧对话框)**:顶栏=返回 + 首页同风格胶囊输入框(cardContainer 底、24dp 圆角、Search 前导图标、清空尾标),提交走输入法搜索键;搜索历史 chips(点击搜索/清空),行尾保留「快搜」入口(跳旧 FastSearchActivity);结果=各源分区(标题 + LazyRow 海报卡,精确匹配排前),分区距顶 16dp、分区间距 24dp(用户定稿);空态/加载态统一组件。勾选源 UI 入口已按用户要求移除(SearchCheckboxDialog/checkedSources 机制保留,按 Hawk SOURCES_FOR_SEARCH 持久化过滤仍生效),拼音联想词未保留(§4.6 未列)。
- **搜索机制**:简化聚合——每源并发限流 6(同旧 SEARCH_THREAD_COUNT),单源 30s 超时防挂起占死令牌;结果接收走 **EventBus TYPE_SEARCH_RESULT**(关键结论:SourceViewModel 对 searchResult 字段的成功/空结果都只发 EventBus,不走 LiveData),以 searchToken+sourceKey 过滤防串;getSearch 的爬虫分支在调用线程阻塞,必须于 IO 线程调用(主线程调用会卡死→ANR);结果列表每分区单 item 稳定 key,禁止在 item 内容里向 LazyListScope 追加 item(会导致闪烁/覆盖)。
- **删除**:旧 SearchActivity.java、activity_search.xml、SearchAdapter、PinyinAdapter;FastSearchActivity 及其依赖保留(快搜)。
- **联调修复记录**:主线程阻塞卡死→IO 调度;永远加载→EventBus 接收;结果后 ANR→重写时丢失 IO 调度(已修);闪烁/覆盖→非法嵌套 item(已修);空 id 崩溃→jumpToDetail 可空兜底(Step 2 补丁)。

### Step 2 实施记录(2026-09-07)

- **入口**:`.ui.activity.MainActivity`(Compose,继承 BaseActivity 以复用 AutoSize 使旧对话框渲染正常;`hideSysBar` 覆写为空,edge-to-edge 由 Compose 接管),布局仅一个 ComposeView。
- **启动引导**:`AppBootstrap`(单例,不持 Activity)移植旧 initData 链:loadConfig → loadJar → Ready;失败弹 Compose AlertDialog(重试/取消=离线继续,"-1" 约定保留);Ready 后按 DEFAULT_LOAD_LIVE 跳直播并预热爬虫。
- **骨架**:Scaffold + NavigationBar(4 tab,选中 primary、底栏 surfaceContainer)+ HorizontalPager(翻页保留各页滚动状态);双击返回退出(2s 提示)。
- **设置 tab**(§4.3 五组全量):播放器/播放体验/首页与搜索/配置与数据/关于;单选项走 AVBoxOptionSheet,开关走 SwitchRow,弹幕 API 走文本输入 sheet;接口配置/接口历史/配置备份/关于复用旧 BaseDialog(挂 ConfigChangedBus);接口线路仅在多线路源时显示;首页分类显示 = 分类开关 sheet(HiddenSorts 按 源+分类名 存 Hawk);检查更新为占位行(v+versionName,点击提示已是最新);快搜=FAST_SEARCH_MODE 开关。
- **历史/收藏 tab**(§4.2 已确认视觉):历史单列列表(64dp 海报+标题/上次看到/时间),收藏双列 2:3 海报网格(scrim+标题);多选管理模式(顶部管理/完成,长按进入并选中;删除选中/清空全部);数据走 RoomDataManger,EventBus TYPE_HISTORY_REFRESH 刷新。
- **首页 tab**(§4.1):假搜索栏(→SearchActivity)+ Tune 源管理(→旧 ApiDialog);直播入口固定行首的源 chips(FilterChip);内容流 = 推荐分区(HOME_REC 三态:站点推荐/历史/豆瓣热播,豆瓣保留当日缓存)+ 全部分区分区(标题行+「全部 >」入口→PartitionListActivity 二级页+LazyRow 卡片;2026-09-09 原 Tune 筛选控件删除);分区三态=骨架 shimmer/空态/加载;第一页并发限流 2(Semaphore),LazyRow 触底加载下一页;筛选 bottom sheet(FlowRow chips→filterSelect 重载该分区);卡片=PressableCard 16dp+2:3 海报+scrim+18sp 标题+14sp 元信息(年/地区/类型),长按弹收藏/搜索 sheet。
- **删除旧实现**:HomeActivity/HistoryActivity/CollectActivity/SettingActivity、GridFragment/UserFragment/ModelSettingFragment、9 个专属 adapter、ConfirmClearDialog/SearchRemoteTvDialog、7 个主布局+8 个孤儿布局(含 activity_push/box_vod_control_view 残留)、5 个孤儿 TV widget;Manifest launcher 切 MainActivity 并移除 4 个旧 Activity 声明。
- **过渡兼容**:播放内核选项含已安装的外部播放器(PlayerHelper.getExistPlayerTypes 点击时计算);action 卡片经 SourceViewModel.action(旧行为)。
- **补丁(2026-09-07)**:修复 MainScreen 未调用 `AppBootstrap.start()` 导致永远停留在加载态的问题;加载/错误页补 surfaceContainer 背景(此前透出 BaseActivity 设置的旧 app_bg 窗口底图)。修复状态栏被隐藏:`AppTheme.NoActionBar` 移除 TV 遗留的 `android:windowFullscreen=true`,`BaseDialog.hideSysBar()` 改为空实现(对话框不再强制隐藏系统栏;播放页沉浸仍由 Detail/LivePlay 的 BaseActivity 原逻辑承担)。修复接口配置不生效:恢复旧契约——`Activity.showApiDialog()` 统一在 onchange 里持久化 `API_URL` 并触发 `AppBootstrap.retry()` 全量重载(HomeViewModel 监听 boot→Ready 刷新首页);首页补「尚未配置订阅接口」引导态;`loadHome()` 每次同步刷新 sources(修复配置后引导态不消失)。修复海报不显示:Coil 不识别 TVBox 海报地址约定(`url@Headers={...}/@Cookie/@User-Agent/@Referer`),且 3.6.2 将 `OkHttpNetworkFetcher` 标记 `@Deprecated(HIDDEN)`(Kotlin 不可见);新增 `ui/components/VodImages`(OkHttp 拦截器剥离附加参数并注入请求头,行为同旧 ImgUtil)+ `util/CoilBridge`(Java 桥调用 HIDDEN 类的 `factory()`)经 `components.add` 注册,App.onCreate 初始化单例。修复点击推荐卡片崩溃:部分源返回的条目无 `id`(Java 平台类型),`jumpToDetail` 参数改可空并兜底空串(与旧 Java 行为一致)。
